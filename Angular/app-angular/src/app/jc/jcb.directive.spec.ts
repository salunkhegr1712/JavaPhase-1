import { JcbDirective } from './jcb.directive';

describe('JcbDirective', () => {
  it('should create an instance', () => {
    const directive = new JcbDirective();
    expect(directive).toBeTruthy();
  });
});
